export enum ButtonColors {
  primary = "primary",
  secondary = "secondary",
  default = "default",
  inherit = "inherit",
}

export enum TextFieldVariant {
  filled = "filled",
  standard = "standard",
  outlined = "outlined",
}
